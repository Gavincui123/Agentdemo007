graph TD
%% ========================================================================
%% 第一层：接入与安全层
%% ========================================================================
subgraph 接入层 [接入与安全层]
A[用户请求] --> A1{基础数据校验} --> A2[速率限制 & 熔断器] --> A3[输入安全过滤器];
A1 -->|失败| Err[统一异常处理器];
end

    %% ========================================================================
    %% 第二层：会话与理解层
    %% ========================================================================
    subgraph 会话层 [会话管理与理解层]
        A3 --> B1{会话路由};
        B1 -->|命中| B2[拉取Redis缓存];
        B1 -->|新建| B3[触发摘要Hook];
        B2 --> B4[用户问题改写] --> B5{改写质量校验} --> B7[标准化Query];
        B3 --> B4;
    end

    %% ========================================================================
    %% 第三层：意图识别与模型路由决策层（核心变动）
    %% ========================================================================
    subgraph 决策层 [意图识别与模型路由层]
        B7 --> C1[多层级意图识别] --> C2{分类校验};
        C2 --> C4{路由分发};
        
        C4 -->|闲聊/简单QA| Route_Simple[简单模型路由];
        C4 -->|推理/分析| Route_Reason[推理模型路由];
        C4 -->|长上下文/复杂| Route_Long[长上下文模型路由];
        C4 -->|结构化抽取| Route_Struct[结构化模型路由];
        
        %% 模型选择策略（新增）
        Route_Simple --> Sel1[查询配置中心];
        Route_Reason --> Sel2[查询配置中心];
        Route_Long --> Sel3[查询配置中心];
        Route_Struct --> Sel4[查询配置中心];
        
        Sel1 --> M{模型选择策略};
        Sel2 --> M;
        Sel3 --> M;
        Sel4 --> M;
        M -->|按标签/权重/成本| M1[选定目标模型];
    end

    %% ========================================================================
    %% 第四层：能力与执行层（RAG/工具/HITL/Agent）
    %% ========================================================================
    subgraph 执行层 [能力与执行层]
        Route_RAG[触发RAG] --> D1[向量检索] --> D2{检索校验} --> D4[Rank重排] --> D5[RAG注入扫描] --> D7[纯净片段];
        Route_Tool[触发工具] --> E1[参数解析] --> E2{Schema校验} --> E4[执行API] --> E5[工具返回数据];
        Route_HITL[高风险] --> F1[HITL确认] --> F2{超时/权限} --> F3[人工工单];
    end

    %% ========================================================================
    %% 第五层：上下文构建工厂（三层隔离 - 不变）
    %% ========================================================================
    subgraph 构建层 [上下文构建工厂 - 三层隔离]
        CB[收集数据] --> 层一{系统锚点层};
        层一 -->|system/runtime| L1[系统提示词 + 运行时元数据];
        L1 --> 层二{客观数据层};
        层二 -->|history/rag/tool| L2[历史摘要 + 安全片段 + 工具结果];
        L2 --> 层三{用户指令层};
        层三 -->|user| L3[改写后用户问题];
        L3 --> CB_Merge[按序拼接: Sys→Runtime→His→RAG→Tool→User];
    end

    %% ========================================================================
    %% 第六层：统一模型网关与配置中心（完全重构）
    %% ========================================================================
    subgraph 推理层 [统一模型网关与多模型治理]
        %% 配置中心（独立子模块）
        subgraph ConfigCenter [模型配置中心 - 热加载]
            Conf[(YAML/Etcd存储)] --> Conf1[模型元数据<br>（端点/Key/上下文窗口）];
            Conf --> Conf2[路由规则<br>（意图↔模型映射）];
            Conf --> Conf3[流控策略<br>（RPM/TPM/成本上限）];
            Conf --> Conf4[容灾策略<br>（主备/重试/超时）];
        end

        CB_Merge --> Gateway[统一模型网关];
        M1 --> Gateway;
        Conf1 -.->|加载配置| Gateway;
        Conf2 -.->|加载路由| Gateway;
        Conf3 -.->|限流| Gateway;
        Conf4 -.->|容灾| Gateway;
        
        Gateway --> GW1{网关拦截逻辑};
        GW1 -->|检查配额| GW2[校验Token预算 & 速率限制];
        GW2 -->|超限| GW3[返回429降级话术];
        GW2 -->|通过| GW4[执行HTTP请求];
        
        GW4 -->|主模型成功| GW5[接收响应];
        GW4 -->|主模型超时/异常| GW6[自动故障转移];
        GW6 --> GW7[切换备选模型] --> GW4;
        GW5 --> H2[LLM原始输出];
    end

    %% ========================================================================
    %% 第七层：结构化输出与安全（不变）
    %% ========================================================================
    H2 --> H3{结构化输出网关} --> H4{JsonSchema校验};
    H4 -->|失败| H5[重试/兜底];
    H4 -->|通过| H6[输出安全过滤器] --> H7[最终响应];

    %% ========================================================================
    %% 异常汇总
    %% ========================================================================
    Err --> Z;
    D7 --> Z;
    F3 --> Z;
    GW3 --> Z;
    H5 --> Z;
    H7 --> O[返回用户];

    %% ========================================================================
    %% 横向支撑体系（新增配置热更新）
    %% ========================================================================
    subgraph 支撑层 [横向支撑体系]
        Obs[全链路可观测性] -.-> D5
        Obs -.-> Gateway
        Obs -.-> Conf
        
        Feedback[在线反馈] --> DataPool[离线数据池];
        DataPool --> Offline[离线微调流水线];
        
        %% 微调结果动态更新配置（核心闭环）
        Offline -->|新模型上线| Deploy[模型注册];
        Deploy -.->|API注册| Conf1;
        
        Admin[运维控制台] -.->|动态调整路由权重| Conf2;
    end